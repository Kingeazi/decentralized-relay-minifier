[project]
name = "decentralized-relay-minifier"
authors = ["Web3 Relay Team"]
description = "An efficient cross-chain message relay optimization system"
telemetry = false
requirements = []
[contracts.relay-minifier]
path = "contracts/relay-minifier.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
